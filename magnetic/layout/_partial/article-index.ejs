{
        "path":<%- JSON.stringify(config.root + item.path ) %>,
        "title":<%- JSON.stringify(item.title) %>,
        "slug":<%- JSON.stringify(item.slug) %>,
        "dates":<%- JSON.stringify(item.date) %>
        
}